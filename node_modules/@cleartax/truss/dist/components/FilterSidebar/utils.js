const convertMinMaxToString = (state)=>{
    const newState = {
        ...state
    };
    Object.keys(newState).forEach((key)=>{
        const filter = newState[key];
        if (void 0 !== filter.min && 'number' == typeof filter.min) filter.min = String(filter.min);
        if (void 0 !== filter.max && 'number' == typeof filter.max) filter.max = String(filter.max);
    });
    return newState;
};
const getInitialYearRange = ()=>{
    const currentYear = new Date().getFullYear();
    return [
        currentYear - 6,
        currentYear + 5
    ];
};
export { convertMinMaxToString, getInitialYearRange };
