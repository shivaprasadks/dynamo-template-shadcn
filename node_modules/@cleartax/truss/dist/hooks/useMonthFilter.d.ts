import { SelectorType } from '../components/FilterSidebar/types';
interface UseMonthFilterProps {
    value?: string;
    onChange: (value: string) => void;
}
interface UseMonthFilterReturn {
    isOpen: boolean;
    selectedDate: Date;
    selectorType: SelectorType;
    yearRange: [number, number];
    displayDate: string;
    handlePopoverOpenChange: (open: boolean) => void;
    handleMonthSelect: (monthIndex: number) => void;
    handleYearSelect: (year: number) => void;
    handlePreviousYearRange: () => void;
    handleNextYearRange: () => void;
    handleYearClick: () => void;
    handleSelectedMonthChange: (date: Date) => void;
}
export declare const useMonthFilter: ({ value, onChange, }: UseMonthFilterProps) => UseMonthFilterReturn;
export {};
