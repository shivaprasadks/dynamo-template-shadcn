import * as React from 'react';
type View = 'calendar' | 'month' | 'year';
export interface UseCalendarV2Props {
    defaultMonth?: Date;
    enableYearSelector?: boolean;
}
export declare function useCalendarV2({ defaultMonth, enableYearSelector, }: UseCalendarV2Props): {
    view: View;
    setView: React.Dispatch<React.SetStateAction<View>>;
    selectedMonth: Date;
    setSelectedMonth: React.Dispatch<React.SetStateAction<Date>>;
    yearRange: [number, number];
    handleCaptionClick: () => void;
    handleYearClick: () => void;
    handleMonthSelect: (month: number) => void;
    handleYearSelect: (year: number) => void;
    handlePreviousYearRange: () => void;
    handleNextYearRange: () => void;
    handlePreviousMonthClick: (displayMonth: Date) => void;
    handleNextMonthClick: (displayMonth: Date) => void;
};
export {};
